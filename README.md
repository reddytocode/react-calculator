# Calculator with React (example)

## Available Scripts

In the project directory, you can run:

### `yarn install`

and then

### `yarn start`

to start the project automatically on `http://localhost:3000/`
